# This programs shows the in-built function calls

# TODO: Test all functions and
# remove the ones that doesn't work!
fig figure{
	x <- [1, 2, 3, 4, 5]
	y <- [5, 4, 3, 2, 1]
	z := maximum(x, y)

	t <- (-PI, PI)
	y := sin(t)
	z := tanh(t)

	plot(t, y, {label='sin'})
	plot(t, z, {label='tanh'})	
}