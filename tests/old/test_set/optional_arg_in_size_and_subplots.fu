fig myfig{
	
	# By default this creates subplots with dimension 1,2
	size <- 2

	# No need to write 0,1 only 1 is enough
	subplot 1{
		y <- x
		x <- 1 to 10
		y <- func x
	}

	y <- x^2
	x <- 2 to 10
	y <- func x

}
